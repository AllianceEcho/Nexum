[package]
name = "nexum-plugin"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Nexum plugin component"

[dependencies]
nexum-domain = { path = "../domain" }
nexum-engine = { path = "../engine" }
nexum-resolver = { path = "../resolver" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
