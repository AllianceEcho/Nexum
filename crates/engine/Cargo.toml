[package]
name = "nexum-engine"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Nexum engine adapter component"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls"] }
nexum-domain = { path = "../domain" }
nexum-task = { path = "../task" }
