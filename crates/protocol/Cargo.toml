[package]
name = "nexum-protocol"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Nexum protocol component"

[dependencies]
nexum-core = { path = "../core" }
nexum-domain = { path = "../domain" }
nexum-task = { path = "../task" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
